Prompt 37a — Part 1 of 2: add futures_symbols/futures_daily tables and
capture real Fyers futures contract symbols during symbol-master sync.
(Part 2, in a separate prompt, will add the actual daily-sync job and API
endpoint that use this data — this prompt only stores the symbols.)

Confirmed via Prompt 36's diagnostic (real NSE_FO.csv row for BANKNIFTY):
  col 3  = lot_size
  col 4  = tick_size
  col 8  = expiry, as a Unix timestamp (seconds)
  col 9  = exact Fyers symbol to use, e.g. "NSE:BANKNIFTY26AUGFUT"
  col 11 = instrument_type code — matches the EXISTING INSTR_TYPE_LABEL
           map already in symbolMasterService.ts ("11"="FUTIDX", "13"="FUTSTK")
  col 13 = underlying symbol, e.g. "BANKNIFTY"
Always use the literal col 9 value from the file rather than constructing
the symbol string, since Fyers is the source of truth.

── Part A: server/src/db/market-schema.ts ───────────────────────────────

Add two new tables, following the exact style/placement of the existing
options_intraday table:

  CREATE TABLE IF NOT EXISTS futures_symbols (
    underlying    TEXT NOT NULL,
    expiry        TEXT NOT NULL,   -- YYYY-MM-DD
    fyers_symbol  TEXT NOT NULL,
    lot_size      INTEGER,
    tick_size     REAL,
    updated_at    TEXT NOT NULL,
    UNIQUE(underlying, expiry)
  );

  CREATE TABLE IF NOT EXISTS futures_daily (
    underlying  TEXT NOT NULL,
    expiry      TEXT NOT NULL,   -- YYYY-MM-DD
    date        TEXT NOT NULL,   -- YYYY-MM-DD
    open        REAL,
    high        REAL,
    low         REAL,
    close       REAL,
    volume      INTEGER,
    UNIQUE(underlying, expiry, date)
  );
  CREATE INDEX IF NOT EXISTS idx_futures_daily
    ON futures_daily(underlying, date);

(futures_daily is created now but stays empty until Part 2's sync job
exists — that's expected, not a bug.)

── Part B: server/src/services/symbolMasterService.ts ───────────────────

Remove the temporary [DEBUG-TEMP] logging block added in Prompt 36.
Replace it with permanent logic: while looping over foLines (the same
loop that currently only builds `foUnderlyings`), also collect rows where
cols[11] is "11" (FUTIDX) or "13" (FUTSTK) into a futures contracts array:

  interface FuturesContractRow {
    underlying: string;
    expiry: string;       // YYYY-MM-DD, converted from the unix timestamp
    fyersSymbol: string;
    lotSize: number;
    tickSize: number;
  }
  const futuresContracts: FuturesContractRow[] = [];

  // Inside the existing foLines loop, alongside the foUnderlyings.add(...) line:
  const instrType = cols[11]?.trim();
  if (instrType === "11" || instrType === "13") {
    const expiryUnix = Number(cols[8]);
    const fyersSymbol = cols[9]?.trim();
    const lotSize = Number(cols[3]);
    const tickSize = Number(cols[4]);
    if (symbol && fyersSymbol && Number.isFinite(expiryUnix) && expiryUnix > 0) {
      futuresContracts.push({
        underlying: symbol,
        expiry: new Date(expiryUnix * 1000).toISOString().slice(0, 10),
        fyersSymbol,
        lotSize: Number.isFinite(lotSize) ? lotSize : 0,
        tickSize: Number.isFinite(tickSize) ? tickSize : 0,
      });
    }
  }

After the loop, log the count and upsert into futures_symbols (place this
right after the existing "F&O underlyings: %d" log line):

  console.log(`[symbol-master] Futures contracts found: ${futuresContracts.length}`);
  const futStmt = marketDb.prepare(`
    INSERT INTO futures_symbols (underlying, expiry, fyers_symbol, lot_size, tick_size, updated_at)
    VALUES (?, ?, ?, ?, ?, ?)
    ON CONFLICT(underlying, expiry) DO UPDATE SET
      fyers_symbol = excluded.fyers_symbol,
      lot_size     = excluded.lot_size,
      tick_size    = excluded.tick_size,
      updated_at   = excluded.updated_at
  `);
  const nowIso = new Date().toISOString();
  for (const fc of futuresContracts) {
    futStmt.run(fc.underlying, fc.expiry, fc.fyersSymbol, fc.lotSize, fc.tickSize, nowIso);
  }

Test: apply, restart, click "Refresh Symbol Master" in Settings, and
confirm the console shows "[symbol-master] Futures contracts found: N"
with N > 0 (expect a few hundred — roughly 3 expiries × ~213 F&O
underlyings). Share that log line — once confirmed, Part 2 adds the
actual sync job and API endpoint that read from these new tables.